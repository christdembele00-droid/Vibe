{
  "name": "vibe-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:web": "set -eu; rm -rf www; mkdir -p www; find . -maxdepth 1 -type f \( -name '*.html' -o -name '*.css' -o -name '*.js' -o -name '*.webmanifest' -o -name '*.png' -o -name '*.jpg' -o -name '*.jpeg' -o -name '*.svg' -o -name '*.ico' -o -name '*.webp' \) ! -name 'package.json' ! -name 'package-lock.json' -exec cp {} www/ \;; for d in assets images icons; do if [ -d \"$d\" ]; then rsync -a \"$d/\" \"www/$d/\"; fi; done",
    "build:android": "npm run prepare:web && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@capacitor-firebase/authentication": "7.5.0",
    "firebase": "11.10.0"
  }
}
