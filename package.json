{
  "name": "vibe-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:web": "npm run build:frontend && rm -rf www && cp -R frontend/out www",
    "build:android": "npm run prepare:web && npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:frontend": "cd frontend && npm install --no-audit --no-fund && npm run build"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3",
    "@capacitor-firebase/authentication": "7.5.0",
    "firebase": "11.10.0",
    "@capacitor-firebase/messaging": "7.5.0"
  }
}
