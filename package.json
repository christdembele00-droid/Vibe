{
  "name": "vibe-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare:web": "rm -rf www && mkdir -p www && rsync -a ./ www/ --exclude='.git' --exclude='.github' --exclude='node_modules' --exclude='android' --exclude='www'",
    "build:android": "npm run prepare:web && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/core": "^7.4.3",
    "@capacitor/android": "^7.4.3",
    "@capacitor/cli": "^7.4.3"
  }
}
