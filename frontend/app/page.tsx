"use client";
import { useState } from "react";
const contacts=[["Amina","On se parle ce soir",true,"A"],["Koffi","Photo reçue",true,"K"],["Mariam","D'accord 👍",false,"M"],["Yao","À demain",false,"Y"]];
const messages=[["Salut ! Tu as vu la nouvelle interface de VIBE ?",false,"20:04"],["Oui. On abandonne les gros panneaux et les bordures partout.",true,"20:05"],["Exactement. Tout doit sembler plus fluide et plus spatial.",true,"20:05"],["Le média est maintenant intégré directement dans la conversation.",false,"20:06"]];
const channels=[["LTA Infos","12,4 k abonnés","L","#544178"],["Tech CI","8,1 k abonnés","T","#0d6254"],["VIBE News","5,7 k abonnés","V","#24384b"],["Campus","3,2 k abonnés","C","#7a4a35"],["Gaming","2,9 k abonnés","G","#3c355e"],["Sciences","1,8 k abonnés","S","#35604c"]];
export default function Home(){const[searchOpen,setSearchOpen]=useState(false);const[aiOpen,setAiOpen]=useState(true);const[followed,setFollowed]=useState(false);const[channelsOpen,setChannelsOpen]=useState(false);const[text,setText]=useState("");
return <main className="vibe-app"><div className="vibe-shell"><aside className={`sidebar ${channelsOpen?"open":""}`}><div className="brand"><div className="brand-mark">V</div><div className="brand-name">VIBE</div></div><div className={`search ${searchOpen?"open":""}`} onClick={()=>setSearchOpen(true)}><span>⌕</span>{searchOpen&&<input autoFocus placeholder="Rechercher" aria-label="Rechercher"/>}</div><div className="section-label">Conversations</div><div className="contact-list">{contacts.map(([name,last,online,initial])=><button className="contact" key={name}><div className="avatar">{initial}{online&&<span className="presence"/>}</div><div className="contact-meta"><div className="contact-name">{name}</div><div className="contact-last">{last}</div></div></button>)}</div><div className="section-label">Explorer</div><button className="contact" onClick={()=>setChannelsOpen(true)}><div className="avatar">#</div><div className="contact-meta"><div className="contact-name">Chaînes</div><div className="contact-last">Découvrir les communautés</div></div></button></aside><section className="main">{!channelsOpen?<div className="chat"><header className="chat-header"><div className="chat-person"><div className="avatar">A<span className="presence"/></div><div><div className="chat-title">Amina</div><div className="chat-status">en ligne</div></div></div><div className="icon-row"><button className="icon-btn" aria-label="Recherche">⌕</button><button className="icon-btn" aria-label="Ouvrir AURORA" onClick={()=>setAiOpen(!aiOpen)}>✦</button><button className="icon-btn" aria-label="Menu">⋯</button></div></header><div className="messages">{messages.map(([msg,me,time],i)=><div className={`message-row ${me?"me":""} ${i>0&&messages[i-1][1]===me?"continued":""}`} key={i}><div className="bubble">{msg}<div className="bubble-meta">{time}</div></div></div>)}</div><div className="composer-wrap"><form className="composer" onSubmit={e=>{e.preventDefault();if(text.trim())setText("")}}><button type="button" className="icon-btn" aria-label="Pièce jointe">＋</button><input value={text} onChange={e=>setText(e.target.value)} placeholder="Écrire un message…"/><button type="button" className="icon-btn" aria-label="Message vocal">◉</button><button className="send" aria-label="Envoyer">{text.trim()?"➤":"⌁"}</button></form></div><aside className={`ai-drawer ${aiOpen?"":"closed"}`}><div className="ai-head"><div><div className="ai-title">AURORA</div><div className="ai-chip">VIBE AI</div></div><button className="icon-btn" aria-label="Fermer" onClick={()=>setAiOpen(false)}>×</button></div><div className="ai-suggestions"><button className="ai-suggestion">Résume cette conversation</button><button className="ai-suggestion">Propose une réponse</button><button className="ai-suggestion">Traduis le dernier message</button><button className="ai-suggestion">Cherche une information</button></div></aside></div>:<section className="channels"><div className="channels-head"><div><div className="section-label" style={{margin:"0 0 6px"}}>Explorer</div><div className="channels-title">Chaînes</div></div><div className={`segmented ${followed?"followed":""}`}><div className="segmented-thumb"/><button onClick={()=>setFollowed(false)}>Toutes</button><button onClick={()=>setFollowed(true)}>Suivies</button></div></div><div className="bento">{channels.filter((_,i)=>!followed||i<3).map(([name,count,logo,tone])=><article className="channel-tile" style={{background:tone}} key={name}><div className="channel-cover"/><div className="channel-logo">{logo}</div><div className="channel-content"><div className="channel-name">{name}</div><div className="channel-followers">{count}</div></div></article>)}</div></section>}</section></div></main>}