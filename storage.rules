rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /chat-media/{roomId}/{fileName} {
      allow read, write: if request.auth != null && roomId.matches('^[A-Za-z0-9_-]+$');
    }
  }
}