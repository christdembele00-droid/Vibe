rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Règles permissives de développement : tout utilisateur authentifié
    // (y compris une session anonyme Firebase) peut lire et écrire.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
